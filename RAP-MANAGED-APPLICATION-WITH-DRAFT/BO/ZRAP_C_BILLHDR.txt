projection implementation in class zbp_rap_c_billhdr unique;
strict ( 2 );
//with draft;
use draft;
use side effects;
define behavior for ZRAP_C_BILLHDR alias BillHdr
{
  use create( precheck );
  use update( precheck );
  use delete;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;


  use action release_doc;
  use action UpdateBillingdate;
  use action CreateSalesDoc;
  use action copy_instance;

  use association _Item { create; with draft; }
}

define behavior for ZRAP_C_BILLITM alias BillItem
{
  use update;
  use delete;

  use association _Hdr { with draft; }
}